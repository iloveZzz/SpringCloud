spring:
    cloud:
        config:
            server:
                encrypt:
                  enabled: false
                git:
                  uri: https://github.com/iloveZzz/spring-config-repo
                  search-paths: app-server,web-client,yml-ceshi
                health:
                  repositories:
                    app-server:
                      label: master
                      name: microserver-app
                      profiles: dev,product

    application:
      name: microservice-config-server
    rabbitmq:
#      host: 192.168.129.201
#      port: 5672
      addresses: 192.168.129.201:5672
      username: xianbei
      password: Xianbei123.
management:
  security:
    enabled: true
server:
  port: 8080
logging:
  level:
    org.springframework.cloud: DEBUG
    org.springframework.boot: INFO
eureka:
  client:
    serviceUrl:
      defaultZone: http://xianbei.viphk.ngrok.org/eureka/
  instance:
    preferIpAddress: true


